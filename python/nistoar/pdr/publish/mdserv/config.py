"""
Utilities for obtaining a configuration for the metadata service

The original implementation has been moved to nistoar.pdr.config; for 
backward compatibility, this module's implementation is pulled in from there 
"""
from ...config import *
from ...config import _log_handler
