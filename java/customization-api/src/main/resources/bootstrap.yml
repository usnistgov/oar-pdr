spring:
  application:
    name: oar-customization-service
  profiles:
    active: local
---
spring:
  application:
    name: oar-customization-service
  profiles: local
  cloud:
    config:
      uri: http://localhost:8087
      
samlauth:
  enabled: false
---
spring:
  application:
    name: oar-customization-service
  profiles: default
  cloud:
    config:
      uri: http://localhost:8087